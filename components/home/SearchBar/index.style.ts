import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    padding: 10,
  },

  input: {
    padding: 10,
    backgroundColor: "#FFF",
    borderRadius: 10,
  },
});

export default styles;
